import 'package:flutter/material.dart';

class CoffeeloverHome extends StatelessWidget {
  @override
  Widget build(BuildContext context) {
    return Scaffold(
      appBar: AppBar(title: Text('Coffeelover Home')),
      body: Center(
        child: Text('Welcome to Coffeelover Microfrontend!'),
      ),
    );
  }
}
